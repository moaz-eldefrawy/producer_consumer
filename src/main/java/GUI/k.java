package GUI;

public class k {
    /*private class Machine{
        void run(){
            register();
        }

        void register(){
            //enqueue yourself in input queues' waiting list
        }

        // if already processing block method call
        void synchronized process(){
            delay();
            enqueueProduct();
            register();
        }
    }
    private class Queue{
        void run(){
            while (products queue empty);

            //we have a product
            pop product;

            //now try to lock a machine
            w
            machine.process();
        }

        synchronized enqueueProduct

        synchronized enqueuMachine
    }*/
}
/*
3 cases
2 machines try to take from same queue: never happens
2 machines try to push in the same queue: synchronized
2 queues try to push in the same machine: Balking will prevent it
*/
